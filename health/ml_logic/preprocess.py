import pandas as pd

def preprocess_data(data: pd.DataFrame) -> pd.DataFrame:
    # Aqui você aplicaria o mesmo processamento que fez durante o treinamento
    # Exemplo: normalização, encoding etc.
    # Certifique-se de que os dados estejam no formato correto para o modelo
    return data
